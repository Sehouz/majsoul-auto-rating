Bundled static data files used by majsoul-auto-rating.

The `tensoul_data.json` file is copied from the local `tensoul` project so the
Tenhou-format report export can translate fan IDs to Japanese names without any
hardcoded host path dependency.
